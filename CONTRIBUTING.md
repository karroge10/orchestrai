# Contributing

Use focused changes, keep every OS action behind the tool registry, and add tests for planning or policy changes. New tools must document risk, approval behavior, reversibility, data access, expected output, and timeouts. Do not add destructive capabilities without a threat-model update and explicit maintainers' review.

Run `npm run typecheck`, `npm test`, and `npm run build` before opening a pull request.
