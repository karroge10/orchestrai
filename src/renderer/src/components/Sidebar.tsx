import { Clock3, Settings as SettingsIcon, Sparkles } from 'lucide-react'
import type { Task } from '../../../shared/types'
export function Sidebar({tasks,selectedId,onSelect,onSettings}:{tasks:Task[];selectedId?:string;onSelect:(id:string)=>void;onSettings:()=>void}){return <aside className="sidebar"><div className="brand"><span><Sparkles size={19}/></span><div><b>OrchestrAI</b></div></div><div className="section-label">Recent tasks</div><nav className="task-list">{tasks.length===0?<div className="empty-small">Your local task history will appear here.</div>:tasks.map((task)=><button className={task.id===selectedId?'active':''} key={task.id} onClick={()=>onSelect(task.id)}><Clock3 size={14}/><span>{task.input}</span><i className={`status-dot ${task.status}`}/></button>)}</nav><button className="settings-link" onClick={onSettings}><SettingsIcon size={16}/>Settings</button><div className="local-badge"><span/>Local-first<br/><small>Actions stay visible</small></div></aside>}
